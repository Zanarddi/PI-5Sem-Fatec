import { Request, Response } from 'express';
import { ParkingSpotModel, ParkingSpot } from '../models/ParkingSpotModel';

export class ParkingSpotController {
  constructor() {
  }

  // TODO: implementar metodos que mudam a intenção de uso da vaga, visto que não há muito mais a ser feito
}